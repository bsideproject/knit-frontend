import styled from '@emotion/styled';
import * as font from '~/styles/font';

export const Container = styled.div`
  ${font.set(12)}
  color: #818189;
  margin-left: 5px;
  position: relative;
  top: 5px;
`;
