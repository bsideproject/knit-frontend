import { FC } from 'react';

import { Thread as ThreadComponent } from '~/organisms/thread';

const ThreadPage: FC = () => {
  return <ThreadComponent id={null} isEditMode />;
};

export default ThreadPage;
