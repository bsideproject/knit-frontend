import styled from '@emotion/styled';
import * as SideBanner from '~/atoms/banner/SideBanner';

export const Container = styled(SideBanner.Container)`
  background: rgba(129, 0, 127, 0.08);
`;

export const { Title, Desc } = SideBanner;
