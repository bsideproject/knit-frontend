import Section from '../Section';
import RecentlyModified from './RecentlyModified';

const ExtraSection = () => {
  return (
    <Section>
      <RecentlyModified />
    </Section>
  );
};

export default ExtraSection;
